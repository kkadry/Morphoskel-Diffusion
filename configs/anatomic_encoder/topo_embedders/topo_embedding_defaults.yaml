logging:
  attributes:
    metric_type: topo
  dim_tags: ['n']
  tissue_keys: ${anatomy.logging.tissue_keys}
training:
  is_trainable: false
  input_key: x
  ucg_rate: 0.0

tensor_embedder:
  emb_vox_shape: ${anatomy.tensor_embedder.emb_vox_shape}
  emb_seq_len: ${anatomy.tensor_embedder.emb_seq_len}
  emb_vec_shape: ${anatomy.tensor_embedder.emb_vec_shape}
  emb_axis: ${anatomy.tensor_embedder.emb_axis}